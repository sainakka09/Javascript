//========String methods of functions=====//
let str = "   sainakka09   ";
console.log(str.toUpperCase()); // toUppercase()
console.log(str.toLowerCase()); //toLowercase()
console.log(str.trim()); //removes white spaces at the end and starting
console.log(str.slice(2)); // returns a part of the string
let a = "sainakka05";
let b = a.concat(str);//join trwo strings
console.log(b)
console.log(str.replace("s","b")); // replaces the value
console.log(str.charAt(5)) // gives the character at the given index

